apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress
spec:
  tls:
    - hosts:
        - dnd4.ru
      secretName: tls-secret
  rules:
    - host: dnd4.ru
      http:
        paths:
          - path: /
            backend:
              serviceName: webui-service
              servicePort: 80
